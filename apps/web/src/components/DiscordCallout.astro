<aside class="discord-callout" aria-labelledby="discord-heading">
  <p class="label">Corrections</p>
  <h2 id="discord-heading">See something wrong?</h2>
  <p>
    <a href="https://discord.gg/8RFTvCWPPv">Join the Discord</a> and ping
    <strong>@PandaXPanther</strong>.
  </p>
</aside>

<style>
  .discord-callout {
    display: grid;
    grid-template-columns: 7rem minmax(0, 1fr) minmax(14rem, 0.8fr);
    gap: 1.5rem;
    align-items: baseline;
    border-block: 1px solid var(--rule-dark);
    padding-block: 1rem;
  }

  h2,
  p {
    margin: 0;
  }

  h2 {
    font-size: clamp(1.35rem, 2.4vw, 1.9rem);
  }

  .label {
    color: var(--ink-soft);
  }

  @media (max-width: 42rem) {
    .discord-callout {
      grid-template-columns: minmax(0, 1fr);
      gap: 0.25rem;
    }
  }
</style>
